CREATE OR REPLACE FUNCTION DDS_IS_NUMBER(p_string IN VARCHAR2)
   RETURN INT
AUTHID CURRENT_USER 
IS
   v_new_num NUMBER;
BEGIN
   v_new_num := TO_NUMBER(p_string);
   RETURN 1;
EXCEPTION
WHEN VALUE_ERROR THEN
   RETURN 0;
END DDS_IS_NUMBER;